name: quds_provider
description: A simple state manager, states now are simple to reach, easy to save and restore.
version: 0.0.6
homepage: https://www.alrazysoft.com
repository: https://github.com/MohammedAsaadAsaad/quds_provider

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.5
  encrypt: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
